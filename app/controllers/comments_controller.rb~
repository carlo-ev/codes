class CommentsController < ApplicationController
  def new
	@comment = Comment.new(params[:comment])
	@comment.code_id = params[:id]
	@comment.save
	redirect_to Code.all.find(@comment.code_id)
  end
end
